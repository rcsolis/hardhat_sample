{
    "name": "hardhat-sample",
    "version": "1.0.0",
    "description": "Hardhat first sample",
    "main": "main.js",
    "scripts": {
        "start": "npx hardhat clean --emoji && npx hardhat compile --emoji && npx hardhat run scripts/deploy.js --emoji --network hardhat",
        "start-ganache": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.js --emoji --network ganache",
        "start-goerli": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.js --emoji --network goerli",
        "test": "npx hardhat test --emoji"
    },
    "author": "Rafael Chavez",
    "license": "ISC",
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.0",
        "@nomiclabs/hardhat-etherscan": "^3.1.4",
        "hardhat": "^2.12.5",
        "hardhat-gas-reporter": "^1.0.9",
        "solidity-coverage": "^0.8.2"
    },
    "dependencies": {
        "dotenv": "^16.0.3"
    }
}
